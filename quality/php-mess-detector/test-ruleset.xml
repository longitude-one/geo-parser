<?xml version="1.0"?>
<ruleset name="My first PHPMD rule set"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>
        My custom rule set that checks my code...
    </description>
    <!-- Import the entire unused code rule set, but UnusedPrivateMethod -->
    <rule ref="rulesets/unusedcode.xml" />
    <!-- Import the entire cyclomatic complexity rule -->
    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveMethodLength"/>
    </rule>
    <rule ref="rulesets/codesize.xml/ExcessiveMethodLength">
        <!-- Set the maximum method length to 158 because of dataProviders -->
        <properties>
            <property name="minimum" value="158"/>
        </properties>
    </rule>
    <!-- Import the entire naming rule set -->
    <rule ref="rulesets/naming.xml" />
    <!-- Import the entire controversial rule set -->
    <rule ref="rulesets/controversial.xml"/>
    <!-- Import the entire design rule set -->
    <rule ref="rulesets/design.xml"/>
</ruleset>
